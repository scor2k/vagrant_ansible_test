input {
        tcp {
                type => "nodejs"
                mode => "client"
                host => "172.16.0.103"
                port => 80
        }
}

output {
        elasticsearch {
                hosts => ["127.0.0.1:9200"]
                index => "logstash-%{+YYYY.MM.dd}"
        }
}
